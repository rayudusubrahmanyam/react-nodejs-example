name: react-build
on:
  push: 
    branches: 
      - develop
env:
  AGE: 39
jobs:
  build:
     runs-on: ubuntu-latest
     env:
       SURNAME: Rayudu
     steps:
       - name: checkout 
         uses: actions/checkout@v2
       - name: custom-step
         run: |
           echo "Hello $SURNAME $MYNAME age $AGE  ${{ secrets.SONAR_TOKEN }}"
           echo '${{ runner.os }}'
         env:
           MYNAME: Subrahmanyam 

